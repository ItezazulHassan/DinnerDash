<% if user_signed_in?%>
  <% if @user.flag%>  
  <div class="row full-height">
  <%=render 'layouts/dashboard_menu' %>
  <div class="col s10 full-height scroll-bar">
    <%=link_to "Manage Items", admin_items_path,:class=>"btn btn-success", :style=>"margin-bottom: 10px, margin-left: 5px" %>
    <%=link_to "Manage Categories", admin_categories_path,:class=>"btn btn-success", :style=>"margin-bottom: 10px, margin-left: 5px" %>
      <div id="show">
        <%= render "administrator/orders" %>
      </div>
  </div>
  </div>
  </div>
  <% else %>
    <div>User is not an Admin</div>
  <% end %>
<% end %>